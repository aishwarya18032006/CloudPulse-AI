{
  "name": "cloudpulse-ai",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:client": "npm run dev --prefix frontend",
    "dev:server": "npm run dev --prefix backend",
    "dev:ml": "python backend/model/app.py -m uvicorn --reload --port 8000 --host 0.0.0.0",
    "dev:all": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"npm run dev:ml\"",
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:test": "npm run db:test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
